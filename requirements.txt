# Core ML
xgboost==2.1.1
lightgbm==4.5.0          # auto_arima wrapper for SARIMA
scikit-learn==1.5.2
scipy>=1.11.0
statsmodels>=0.14.2
pmdarima>=2.0.4

# Data
pandas==2.2.3
numpy==1.26.4

# Storage
pymongo==4.9.2
joblib==1.4.2

# HTTP (weather fetch + Ollama calls)
pyarrow==17.0.0
requests==2.32.3

# API (Phase 1 Flask — install now to avoid conflicts later)
flask==3.1.0
flask-jwt-extended==4.7.1
gunicorn==23.0.0
flask-cors

# Torch (install stub now; full LSTM in Phase 4)
# Uncomment if you have the GPU RAM. Otherwise leave commented for Phase 1.
# torch==2.4.1
#AI Agent
groq